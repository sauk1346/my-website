# Clase de Python con deserialización de información

## ¿Qué es?
La deserialización nos permite tomar datos en formato JSON o XML, y convertirlos en objetos que Python puede entender y manipular fácilmente.

## json.dumps()
Toma un objeto python (diccionario, lista) y lo convierte en una cadena de texto JSON

```python
import json

datos = {"nombre": "Armando", "edad": 24, "ciudad": "La Serena"}
cadena_json = json.dumps(datos, indent=4)
print(cadena_json)
```


## json.loads()
Toma una cadena de texto JSON y lo convierte en un objeto Python

```python
import json

cadena_json = '{"nombre": "Armando", "edad": 24, "ciudad": "La Serena"}'
datos = json.loads(cadena_json)
print(datos)
```

## json.dump()
Similar a `json.dumps()`. Pero además permite guardar la conversión en un archivo

```python
import json

datos = {"nombre": "Armando", "edad": 24, "ciudad": "La Serena"}
with open("datos.json", "w") as archivo:
	json.dump(datos.archivo)
```

# Actividad de Práctica 12

1. **De los siguientes enunciados, ¿cuál es el propósito principal de la deserialización de información en Python?**
	1. <span style={{color:'red'}}>Convertir datos en formato JSON en objetos Python</span>
	2. Convertir objetos Python en formato JSON
	3. Convertir datos JSON en datos XML
	4. Convertir objetos Python en cadenas de texto JSON

2. **De los siguientes enunciados, ¿cuál es la función de la biblioteca json que se utiliza para convertir un objeto Python en una cadena de texto JSON?**
	1. json.loads()
	2. json.load()
	3. <span style={{color:'red'}}>json.dumps()</span>
	4. json.dump()

3. **¿Cuál es la función de la biblioteca json que se utiliza para cargar datos desde un archivo JSON?**
	1. json.loads()
	2. json.dumps()
	3. json.dump()
	4. <span style={{color:'red'}}>json.load()</span>

4. **¿Qué función de la biblioteca json se utiliza para convertir una cadena de texto JSON en un objeto Python?**
	1. json.dumps()
	2. <span style={{color:'red'}}>json.loads()</span>
	3. json.dump()
	4. json.load()

5. **De los siguientes enunciados, ¿cuál es el principal propósito de la deserialización de información en el contexto de la creación de clases en Python?**
	1. Convertir objetos Python en formato JSON
	2. Convertir objetos Python en cadenas de texto JSON
	3. <span style={{color:'red'}}>Convertir datos JSON en objetos Python para su manipulación en el código</span>
	4. Convertir datos JSON en datos XML
