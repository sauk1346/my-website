# Partición Igualitaria de Árbol

Dado la `raíz` de un árbol binario, devuelve `true` *si puedes particionar el árbol en dos árboles con sumas iguales de valores después de eliminar exactamente una arista del árbol original.*

## Ejemplos

**Ejemplo 1:**

<img
	src="/leetcode/fig19.jpg"
	style={{ display: 'block', margin: '0 auto', width:'60%'}}
/>
<br/>
> **Input:** root = [5,10,10,null,null,2,3]
> **Output:** true

**Ejemplo 2:**
<img
	src="/leetcode/fig20.jpg"
	style={{ display: 'block', margin: '0 auto', width:'25%'}}
/>
<br/>
> **Input:** root = [1,2,10,null,null,2,20]
> **Output:** false
> **Explicación:** No puedes dividir el árbol en dos árboles con sumas iguales después de eliminar exactamente una arista del árbol.

## Soluciones

<details>
    <summary>Python</summary>
```python
class Solution:
    def checkEqualTree(self, root: Optional[TreeNode]) -> bool:
        subtree_sums = []
        
        def dfs(node):
            if not node:
                return 0
            
            left_sum = dfs(node.left)
            right_sum = dfs(node.right)
            current_sum = node.val + left_sum + right_sum
            
            # Guardar suma de todos los subárboles excepto la raíz
            if node != root:
                subtree_sums.append(current_sum)
            
            return current_sum
        
        total_sum = dfs(root)
        
        # Si es impar, imposible dividir en partes iguales
        if total_sum % 2 != 0:
            return False
```
</details>